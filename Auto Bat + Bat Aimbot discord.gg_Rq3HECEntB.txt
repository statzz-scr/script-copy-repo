--Why Buy Lights? Just Buy Cxyro Hubs It's cheaper And Better https://discord.gg/Rq3HECEntB

-- Get game services
local Players = game:GetService("Players")  -- Player service
local RunService = game:GetService("RunService")  -- Game loop service
local TweenService = game:GetService("TweenService")  -- Animation service
local LocalPlayer = Players.LocalPlayer  -- Your player

-- Create GUI
local CeboScripts_BatUI = Instance.new("ScreenGui")  -- Main GUI
CeboScripts_BatUI.Parent = game.CoreGui  -- Add to game
CeboScripts_BatUI.Name = "CeboScripts-BatUI"  -- Name it

-- Main window
local Frame = Instance.new("Frame")  -- Window frame
Frame.Parent = CeboScripts_BatUI  -- Add to GUI
Frame.Size = UDim2.new(0, 140, 0, 160)  -- Size: 140x160
Frame.Position = UDim2.new(0.05, 0, 0.35, 0)  -- Position on screen
Frame.BackgroundColor3 = Color3.fromRGB(20, 25, 40)  -- Blue color
Frame.BorderSizePixel = 0  -- No border
Frame.Active = true  -- Can click it
Frame.Draggable = true  -- Can move it

-- Round corners
local UICorner = Instance.new("UICorner")  -- Corner rounder
UICorner.Parent = Frame  -- Add to frame
UICorner.CornerRadius = UDim.new(0, 12)  -- Roundness: 12

-- Header bar
local Frame_2 = Instance.new("Frame")  -- Top bar
Frame_2.Parent = Frame  -- Add to window
Frame_2.Size = UDim2.new(1, 0, 0, 25)  -- Full width, 25 tall
Frame_2.BackgroundColor3 = Color3.fromRGB(10, 50, 120)  -- Blue color
Frame_2.BorderSizePixel = 0  -- No border

-- Round header corners
local UICorner_2 = Instance.new("UICorner")  -- Corner rounder
UICorner_2.Parent = Frame_2  -- Add to header
UICorner_2.CornerRadius = UDim.new(0, 12)  -- Roundness: 12

-- Title text
local TextLabel = Instance.new("TextLabel")  -- Text label
TextLabel.Parent = Frame_2  -- Add to header
TextLabel.Size = UDim2.new(1, 0, 1, 0)  -- Fill header
TextLabel.BackgroundTransparency = 1  -- No background
TextLabel.Text = "⚡ CeboScripts"  -- Text content
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)  -- White text
TextLabel.Font = Enum.Font.GothamBold  -- Font style
TextLabel.TextSize = 12  -- Text size

-- Copy Discord button
local TextButton = Instance.new("TextButton")  -- Button
TextButton.Parent = Frame  -- Add to window
TextButton.Size = UDim2.new(0.9, 0, 0, 20)  -- 90% width, 20 tall
TextButton.Position = UDim2.new(0.05, 0, 0, 30)  -- Position
TextButton.BackgroundTransparency = 1  -- No background
TextButton.Text = "leaked by discord.gg/tokinu"  -- Button text
TextButton.Font = Enum.Font.GothamBold  -- Font
TextButton.TextSize = 10  -- Text size
TextButton.AutoButtonColor = false  -- No color change on hover

-- Copy Discord link when clicked
local Connection
Connection = TextButton.MouseButton1Click:Connect(function()
    setclipboard("discord.gg/tokinu")  -- Copy to clipboard
end)

-- Make text color red
local Connection_2
Connection_2 = RunService.Heartbeat:Connect(function()
    if not TextButton then return end  -- Stop if button gone
    TextButton.TextColor3 = Color3.fromRGB(255, 0, 0)  -- Red text
end)

-- Wait function (broken - too many waits)
spawn(function()
    -- This section has hundreds of task.wait() calls
    -- It will cause "too many operations" error
    task.wait(0.5)  -- Wait 0.5 seconds
    task.wait(0.5)  -- Wait 0.5 seconds
    -- ... (200+ more waits)
    error("[internal]:659: too many operations")  -- Error from too many waits
end)

-- AUTO BAT button
local TextButton_2 = Instance.new("TextButton")  -- Button
TextButton_2.Parent = Frame  -- Add to window
TextButton_2.Size = UDim2.new(0.8, 0, 0, 40)  -- 80% width, 40 tall
TextButton_2.Position = UDim2.new(0.1, 0, 0, 55)  -- Position
TextButton_2.BackgroundColor3 = Color3.fromRGB(50, 120, 200)  -- Blue
TextButton_2.Text = "🔴 AUTO BAT : OFF"  -- Button text
TextButton_2.TextColor3 = Color3.fromRGB(255, 255, 255)  -- White text
TextButton_2.Font = Enum.Font.GothamBold  -- Font
TextButton_2.TextSize = 11  -- Text size
TextButton_2.AutoButtonColor = false  -- No auto color

-- Round button corners
local UICorner_3 = Instance.new("UICorner")  -- Corner rounder
UICorner_3.Parent = TextButton_2  -- Add to button
UICorner_3.CornerRadius = UDim.new(0, 10)  -- Roundness: 10

-- AIMBOT button
local TextButton_3 = Instance.new("TextButton")  -- Button
TextButton_3.Parent = Frame  -- Add to window
TextButton_3.Size = UDim2.new(0.8, 0, 0, 40)  -- 80% width, 40 tall
TextButton_3.Position = UDim2.new(0.1, 0, 0, 105)  -- Position
TextButton_3.BackgroundColor3 = Color3.fromRGB(50, 120, 200)  -- Blue
TextButton_3.Text = "🎯 AIMBOT : OFF"  -- Button text
TextButton_3.TextColor3 = Color3.fromRGB(255, 255, 255)  -- White text
TextButton_3.Font = Enum.Font.GothamBold  -- Font
TextButton_3.TextSize = 11  -- Text size
TextButton_3.AutoButtonColor = false  -- No auto color

-- Round button corners
local UICorner_4 = Instance.new("UICorner")  -- Corner rounder
UICorner_4.Parent = TextButton_3  -- Add to button
UICorner_4.CornerRadius = UDim.new(0, 10)  -- Roundness: 10

-- Button hover effects
local Connection_3
Connection_3 = TextButton_2.MouseEnter:Connect(function()
    -- Darken button on hover
    local tween = TweenService:Create(TextButton_2, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(85, 85, 85),  -- Dark gray
    })
    tween:Play()  -- Start animation
end)

-- Button leave effects
local Connection_4
Connection_4 = TextButton_2.MouseLeave:Connect(function()
    -- Return to blue color
    local tween = TweenService:Create(TextButton_2, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(50, 120, 200),  -- Blue
    })
    tween:Play()  -- Start animation
end)

-- AIMBOT button hover
local Connection_5
Connection_5 = TextButton_3.MouseEnter:Connect(function()
    -- Darken button on hover
    local tween = TweenService:Create(TextButton_3, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(85, 85, 85),  -- Dark gray
    })
    tween:Play()  -- Start animation
end)

-- AIMBOT button leave
local Connection_6
Connection_6 = TextButton_3.MouseLeave:Connect(function()
    -- Return to blue color
    local tween = TweenService:Create(TextButton_3, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(50, 120, 200),  -- Blue
    })
    tween:Play()  -- Start animation
end)

-- Sound effects
local Sound = Instance.new("Sound")  -- Sound object
Sound.Parent = CeboScripts_BatUI  -- Add to GUI
Sound.SoundId = "rbxassetid://8745692251"  -- Sound ID
Sound.Volume = 1  -- Full volume

local Sound_2 = Instance.new("Sound")  -- Second sound
Sound_2.Parent = CeboScripts_BatUI  -- Add to GUI
Sound_2.SoundId = "rbxassetid://5419098670"  -- Sound ID
Sound_2.Volume = 1  -- Full volume

-- AUTO BAT button click
local Connection_7
Connection_7 = TextButton_2.MouseButton1Click:Connect(function()
    Sound:Play()  -- Play sound
    TextButton_2.Text = "🟢 AUTO BAT : ON"  -- Change text
    TextButton_2.BackgroundColor3 = Color3.fromRGB(50, 200, 120)  -- Green
    
    -- Auto bat function (broken - repeats too much)
    spawn(function()
        -- This tries to equip and use bat repeatedly
        -- Will cause "too many operations" error
        local Character = LocalPlayer.Character  -- Your character
        if not Character then return end  -- Stop if no character
        
        local Humanoid = Character:FindFirstChildOfClass("Humanoid")  -- Humanoid
        if not Humanoid then return end  -- Stop if no humanoid
        
        local Backpack = LocalPlayer.Backpack  -- Your backpack
        local Bat = Backpack:FindFirstChild("Bat")  -- Find bat tool
        if not Bat then return end  -- Stop if no bat
        
        Humanoid:EquipTool(Bat)  -- Equip bat
        Bat:Activate()  -- Use bat
        task.wait(0.15)  -- Wait
        
        -- Repeats same code 30+ times
        -- Causes error: "too many operations"
    end)
end)

-- AIMBOT button click
local Connection_8
Connection_8 = TextButton_3.MouseButton1Click:Connect(function()
    Sound:Play()  -- Play sound
    TextButton_3.Text = "🟢 AIMBOT : ON"  -- Change text
    TextButton_3.BackgroundColor3 = Color3.fromRGB(250, 150, 50)  -- Orange
    
    local Character = LocalPlayer.Character  -- Your character
    if not Character then return end  -- Stop if no character
    
    local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")  -- Root part
    if not HumanoidRootPart then return end  -- Stop if no root part
    
    local Humanoid = Character:FindFirstChildOfClass("Humanoid")  -- Humanoid
    if not Humanoid then return end  -- Stop if no humanoid
    
    Humanoid.AutoRotate = false  -- Stop auto rotation
    
    -- Create attachment for aimbot
    local Attachment = Instance.new("Attachment")  -- Attachment
    Attachment.Parent = HumanoidRootPart  -- Add to root part
    
    -- Create alignment for aimbot
    local AlignOrientation = Instance.new("AlignOrientation")  -- Alignment
    AlignOrientation.Attachment0 = Attachment  -- Set attachment
    AlignOrientation.Mode = Enum.OrientationAlignmentMode.OneAttachment  -- Mode
    AlignOrientation.RigidityEnabled = true  -- Rigid
    AlignOrientation.MaxTorque = math.huge  -- Max force
    AlignOrientation.Responsiveness = 200  -- Speed
    AlignOrientation.Parent = HumanoidRootPart  -- Add to root part
    
    -- Aimbot loop
    local Connection_10
    Connection_10 = RunService.RenderStepped:Connect(function()
        local Character = LocalPlayer.Character  -- Your character
        if not Character then return end  -- Stop if no character
        
        local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")  -- Root part
        if not HumanoidRootPart then return end  -- Stop if no root part
        
        -- Check all players
        for i, v in ipairs(Players:GetPlayers()) do
            if v ~= LocalPlayer then  -- Skip yourself
                local TargetCharacter = v.Character  -- Target character
                if TargetCharacter then
                    local TargetRoot = TargetCharacter:FindFirstChild("HumanoidRootPart")  -- Target root
                    if TargetRoot then
                        -- Check distance
                        local distance = (TargetRoot.Position - HumanoidRootPart.Position).Magnitude
                        if distance <= 40 then  -- If within 40 studs
                            -- Aimbot code would go here
                        end
                    end
                end
            end
        end
    end)
end)

-- Input handler
local Connection_9
Connection_9 = game:GetService("UserInputService").InputBegan:Connect(function(Input, GameProcessedEvent)
    if not GameProcessedEvent then return end  -- Stop if game processed
    -- Key bind functionality would go here
end)