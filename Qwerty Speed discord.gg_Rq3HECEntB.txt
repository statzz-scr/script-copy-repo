--Why Buy Lights? Just Buy Cxyro Hubs It's cheaper And Better https://discord.gg/Rq3HECEntB

-- Get services
local TweenService = game:GetService("TweenService")  -- Animation service
local LocalPlayer = game:GetService("Players").LocalPlayer  -- Your player

-- Create main GUI
local Qwery_Speed_GUI = Instance.new("ScreenGui")  -- Main GUI
Qwery_Speed_GUI.Name = "Qwery_Speed_GUI"  -- Name it
Qwery_Speed_GUI.ResetOnSpawn = false  -- Don't reset on respawn
Qwery_Speed_GUI.Parent = LocalPlayer:WaitForChild("PlayerGui")  -- Add to player GUI

-- Create main window
local Qwery_Main = Instance.new("Frame")  -- Main window
Qwery_Main.Name = "Qwery_Main"  -- Name it
Qwery_Main.Size = UDim2.new(0, 240, 0, 130)  -- Size: 240x130
Qwery_Main.Position = UDim2.new(0.5, -120, 0.4, 0)  -- Center position
Qwery_Main.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  -- Dark background
Qwery_Main.BorderSizePixel = 0  -- No border
Qwery_Main.ClipsDescendants = true  -- Clip child elements
Qwery_Main.Parent = Qwery_Speed_GUI  -- Add to GUI

-- Round corners
local UICorner = Instance.new("UICorner")  -- Corner rounder
UICorner.Parent = Qwery_Main  -- Add to window
UICorner.CornerRadius = UDim.new(0, 10)  -- Roundness: 10

-- Add gradient background
local UIGradient = Instance.new("UIGradient")  -- Gradient
UIGradient.Color = ColorSequence.new({  -- Gradient colors
    ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 0, 40)),  -- Dark purple
    ColorSequenceKeypoint.new(1, Color3.fromRGB(60, 0, 130))  -- Purple
})
UIGradient.Rotation = 45  -- Angle
UIGradient.Parent = Qwery_Main  -- Add to window

-- Handle window clicks
local Connection
Connection = Qwery_Main.InputBegan:Connect(function(Input)
    -- Check for mouse or touch input
    if Input.UserInputType == Enum.UserInputType.MouseButton1 then
        -- Handle mouse click
    end
    if Input.UserInputType == Enum.UserInputType.Touch then
        -- Handle touch
    end
end)

-- Handle window drag
local Connection_2
Connection_2 = Qwery_Main.InputChanged:Connect(function(Input)
    -- Check for mouse or touch movement
    if Input.UserInputType == Enum.UserInputType.MouseMovement then
        -- Handle mouse drag
    end
    if Input.UserInputType == Enum.UserInputType.Touch then
        -- Handle touch drag
    end
end)

-- Handle global input
local Connection_3
Connection_3 = game:GetService("UserInputService").InputChanged:Connect(function(Input)
    -- Input handler (empty)
end)

-- Title label
local TextLabel = Instance.new("TextLabel")  -- Title
TextLabel.Size = UDim2.new(1, -80, 0, 35)  -- Size
TextLabel.Position = UDim2.new(0, 12, 0, 0)  -- Position
TextLabel.BackgroundTransparency = 1  -- No background
TextLabel.Text = "Qwery Speed"  -- Text
TextLabel.TextColor3 = Color3.fromRGB(180, 100, 255)  -- Purple text
TextLabel.Font = Enum.Font.GothamBold  -- Font
TextLabel.TextSize = 14  -- Text size
TextLabel.TextXAlignment = Enum.TextXAlignment.Left  -- Left align
TextLabel.Parent = Qwery_Main  -- Add to window

-- Minimize button
local TextButton = Instance.new("TextButton")  -- Min button
TextButton.Size = UDim2.new(0, 25, 0, 25)  -- Size: 25x25
TextButton.Position = UDim2.new(1, -30, 0, 5)  -- Top right
TextButton.BackgroundTransparency = 1  -- No background
TextButton.Text = "–"  -- Minus symbol
TextButton.TextColor3 = Color3.fromRGB(180, 100, 255)  -- Purple
TextButton.Font = Enum.Font.GothamBold  -- Font
TextButton.TextSize = 20  -- Text size
TextButton.Parent = Qwery_Main  -- Add to window

-- Toggle switch frame
local Frame_2 = Instance.new("Frame")  -- Toggle frame
Frame_2.Size = UDim2.new(0, 35, 0, 18)  -- Size: 35x18
Frame_2.Position = UDim2.new(1, -70, 0, 9)  -- Position
Frame_2.BackgroundColor3 = Color3.fromRGB(30, 30, 30)  -- Dark gray
Frame_2.Parent = Qwery_Main  -- Add to window

-- Round toggle frame
local UICorner_2 = Instance.new("UICorner")  -- Corner rounder
UICorner_2.Parent = Frame_2  -- Add to toggle
UICorner_2.CornerRadius = UDim.new(1, 0)  -- Full round (pill shape)

-- Toggle button
local TextButton_2 = Instance.new("TextButton")  -- Toggle button
TextButton_2.Size = UDim2.new(1, 0, 1, 0)  -- Fill toggle
TextButton_2.BackgroundTransparency = 1  -- No background
TextButton_2.Text = ""  -- No text
TextButton_2.Parent = Frame_2  -- Add to toggle frame

-- Toggle slider
local Frame_3 = Instance.new("Frame")  -- Slider circle
Frame_3.Size = UDim2.new(0, 14, 0, 14)  -- Size: 14x14
Frame_3.Position = UDim2.new(0, 2, 0.5, 0)  -- Left side
Frame_3.AnchorPoint = Vector2.new(0, 0.5)  -- Center vertically
Frame_3.BackgroundColor3 = Color3.new(1, 1, 1)  -- White
Frame_3.Parent = Frame_2  -- Add to toggle frame

-- Round slider
local UICorner_3 = Instance.new("UICorner")  -- Corner rounder
UICorner_3.Parent = Frame_3  -- Add to slider
UICorner_3.CornerRadius = UDim.new(1, 0)  -- Full circle

-- Content frame
local Frame_4 = Instance.new("Frame")  -- Content area
Frame_4.Size = UDim2.new(1, 0, 1, -35)  -- Fill below title
Frame_4.Position = UDim2.new(0, 0, 0, 35)  -- Below title
Frame_4.BackgroundTransparency = 1  -- No background
Frame_4.Parent = Qwery_Main  -- Add to window

-- Speed label
local TextLabel_2 = Instance.new("TextLabel")  -- Speed text
TextLabel_2.Size = UDim2.new(0, 60, 0, 30)  -- Size
TextLabel_2.Position = UDim2.new(0, 15, 0, 10)  -- Position
TextLabel_2.BackgroundTransparency = 1  -- No background
TextLabel_2.Text = "Speed"  -- Text
TextLabel_2.TextColor3 = Color3.fromRGB(180, 100, 255)  -- Purple
TextLabel_2.Font = Enum.Font.GothamSemibold  -- Font
TextLabel_2.TextSize = 14  -- Text size
TextLabel_2.TextXAlignment = Enum.TextXAlignment.Left  -- Left align
TextLabel_2.Parent = Frame_4  -- Add to content

-- Speed input box
local TextBox = Instance.new("TextBox")  -- Speed input
TextBox.Size = UDim2.new(0, 100, 0, 25)  -- Size: 100x25
TextBox.Position = UDim2.new(0, 95, 0, 12)  -- Position
TextBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)  -- Black
TextBox.BackgroundTransparency = 0.5  -- Semi-transparent
TextBox.Text = "56"  -- Default value
TextBox.TextColor3 = Color3.fromRGB(200, 150, 255)  -- Light purple
TextBox.Font = Enum.Font.Gotham  -- Font
TextBox.TextSize = 13  -- Text size
TextBox.Parent = Frame_4  -- Add to content

-- Round input corners
Instance.new("UICorner").Parent = TextBox  -- Add corners

-- Jump label
local TextLabel_3 = Instance.new("TextLabel")  -- Jump text
TextLabel_3.Size = UDim2.new(0, 60, 0, 30)  -- Size
TextLabel_3.Position = UDim2.new(0, 15, 0, 45)  -- Position
TextLabel_3.BackgroundTransparency = 1  -- No background
TextLabel_3.Text = "Jump"  -- Text
TextLabel_3.TextColor3 = Color3.fromRGB(180, 100, 255)  -- Purple
TextLabel_3.Font = Enum.Font.GothamSemibold  -- Font
TextLabel_3.TextSize = 14  -- Text size
TextLabel_3.TextXAlignment = Enum.TextXAlignment.Left  -- Left align
TextLabel_3.Parent = Frame_4  -- Add to content

-- Jump input box
local TextBox_2 = Instance.new("TextBox")  -- Jump input
TextBox_2.Size = UDim2.new(0, 100, 0, 25)  -- Size: 100x25
TextBox_2.Position = UDim2.new(0, 95, 0, 47)  -- Position
TextBox_2.BackgroundColor3 = Color3.fromRGB(0, 0, 0)  -- Black
TextBox_2.BackgroundTransparency = 0.5  -- Semi-transparent
TextBox_2.Text = "75"  -- Default value
TextBox_2.TextColor3 = Color3.fromRGB(200, 150, 255)  -- Light purple
TextBox_2.Font = Enum.Font.Gotham  -- Font
TextBox_2.TextSize = 13  -- Text size
TextBox_2.Parent = Frame_4  -- Add to content

-- Round input corners
Instance.new("UICorner").Parent = TextBox_2  -- Add corners

-- Tween animation info
local _call14 = TweenInfo.new(0.2, Enum.EasingStyle.Quad)  -- Animation settings

-- Minimize button click
local Connection_4
Connection_4 = TextButton.MouseButton1Click:Connect(function()
    -- Shrink window to 35 height
    local tween = TweenService:Create(Qwery_Main, _call14, {
        Size = UDim2.new(0, 240, 0, 35),  -- Small size
    })
    tween:Play()  -- Play animation
    TextButton.Text = "+"  -- Change to plus symbol
end)

-- Toggle button click
local Connection_5
Connection_5 = TextButton_2.MouseButton1Click:Connect(function()
    -- Animate toggle to ON position
    local tween1 = TweenService:Create(Frame_2, _call14, {
        BackgroundColor3 = Color3.fromRGB(150, 0, 255),  -- Purple color
    })
    tween1:Play()  -- Play animation
    
    local tween2 = TweenService:Create(Frame_3, _call14, {
        Position = UDim2.new(1, -16, 0.5, 0),  -- Move to right
    })
    tween2:Play()  -- Play animation
end)

-- Speed input changed
local Connection_6
Connection_6 = TextBox.FocusLost:Connect(function(EnterPressed)
    local speedValue = tonumber(TextBox.Text)  -- Convert to number
    if speedValue then
        -- Apply speed value here
    end
end)

-- Jump input changed
local Connection_7
Connection_7 = TextBox_2.FocusLost:Connect(function(EnterPressed)
    local jumpValue = tonumber(TextBox_2.Text)  -- Convert to number
    if jumpValue then
        -- Apply jump value here
    end
end)

-- Game loop for applying speed/jump
local Connection_8
Connection_8 = game:GetService("RunService").Heartbeat:Connect(function()
    local Character = LocalPlayer.Character  -- Your character
    if not Character then return end  -- Stop if no character
    
    local Humanoid = Character:FindFirstChildOfClass("Humanoid")  -- Humanoid
    if not Humanoid then return end  -- Stop if no humanoid
    
    local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")  -- Root part
    if not HumanoidRootPart then return end  -- Stop if no root part
    
    -- Apply jump power
    local jumpValue = tonumber(TextBox_2.Text)  -- Get jump value
    if jumpValue then
        Humanoid.UseJumpPower = true  -- Enable custom jump
        Humanoid.JumpPower = jumpValue  -- Set jump power
    end
    
    -- Check if moving
    if Humanoid.MoveDirection.Magnitude > 0 then
        -- Apply speed here
        local speedValue = tonumber(TextBox.Text)  -- Get speed value
        if speedValue then
            -- Speed code would go here
        end
    end
end)